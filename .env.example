# Copy to .env and fill your values. Keep this file private.

# Live data provider (auto if not set). Options: polygon | mock
VITE_PROVIDER=

# Polygon API key for live data (stocks and crypto, e.g., X:BTCUSD)
VITE_POLYGON_KEY=

# Refresh interval in milliseconds (default 60000)
VITE_REFRESH_MS=
